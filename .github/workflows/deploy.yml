# Automatic GitHub Pages deploy was removed: static export (`output: "export"`)
# is incompatible with Next.js Route Handlers (`src/app/api`). Production deploy
# uses Vercel — see README.

name: GitHub Pages (deprecated)

on:
  workflow_dispatch:

jobs:
  reminder:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy on Vercel instead
        run: |
          echo "::notice title=Deploy::Connect this repo to Vercel — see README."
          echo "Static export cannot coexist with src/app/api Route Handlers."
